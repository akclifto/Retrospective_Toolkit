/*
Placeholder for now
function admin(req, res, next) {
    if(check for permissions) {
        next(err);
    }
    res.json(req.session); //just for testing purposes
}

module.exports = {
    admin
};*/